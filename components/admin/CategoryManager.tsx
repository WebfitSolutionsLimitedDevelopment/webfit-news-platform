'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
function slugify(v:string){return v.toLowerCase().normalize('NFKD').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')}
export default function CategoryManager({categories}:{categories:any[]}){const router=useRouter();const[name,setName]=useState('');const[slug,setSlug]=useState('');const[msg,setMsg]=useState('');const[busy,setBusy]=useState(false);async function create(){setBusy(true);setMsg('');try{const r=await fetch('/api/admin/categories',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,slug:slug||slugify(name),is_active:true})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Could not create category');setName('');setSlug('');setMsg('Category created.');router.refresh()}catch(e:any){setMsg(e.message)}finally{setBusy(false)}}async function toggle(id:string,value:boolean){await fetch(`/api/admin/categories/${id}`,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({is_active:value})});router.refresh()}return <><div className="admin-card category-create"><h2>Add category</h2><div className="category-create-row"><input placeholder="Category name" value={name} onChange={e=>{setName(e.target.value);setSlug(slugify(e.target.value))}}/><input placeholder="slug" value={slug} onChange={e=>setSlug(slugify(e.target.value))}/><button className="admin-primary" disabled={busy||name.length<2} onClick={create}>Add</button></div>{msg&&<p className="admin-note">{msg}</p>}</div><div className="admin-card admin-table-wrap"><table><thead><tr><th>Name</th><th>Slug</th><th>Parent</th><th>Status</th><th>Archive ID</th></tr></thead><tbody>{categories.map(c=><tr key={c.id}><td><strong>{c.name}</strong></td><td><code>{c.slug}</code></td><td>{c.parent?.name||'/'}</td><td><label className="toggle"><input type="checkbox" checked={c.is_active} onChange={e=>toggle(c.id,e.target.checked)}/><span>{c.is_active?'Active':'Hidden'}</span></label></td><td>{c.wp_term_id||'Native'}</td></tr>)}</tbody></table></div></>}
