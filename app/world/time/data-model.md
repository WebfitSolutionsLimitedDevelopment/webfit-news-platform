# World Time data model

No remote API. Uses IANA time-zone identifiers and `Intl.DateTimeFormat` in the client runtime. Live clocks update each second and DST follows the runtime time-zone database.
