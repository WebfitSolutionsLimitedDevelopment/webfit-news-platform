import { NextResponse } from 'next/server';
import { z } from 'zod';
import { createClient } from '../../../../../lib/supabase-server';

const Input=z.object({campaign_id:z.string().uuid(),media_id:z.string().uuid().nullable(),headline:z.string().max(160).nullable(),destination_url:z.string().url(),alt_text:z.string().max(240).nullable()});
async function context(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return null;const {data:p}=await supabase.from('profiles').select('role,is_active').eq('id',user.id).maybeSingle();if(!p?.is_active||!['super_admin','editor','ad_manager'].includes(p.role))return null;return{supabase,user};}
export async function POST(req:Request){const c=await context();if(!c)return NextResponse.json({error:'Advertising permission required'},{status:403});const parsed=Input.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'Invalid creative',details:parsed.error.flatten()},{status:422});const {data,error}=await c.supabase.from('ad_creatives').insert(parsed.data).select('*').single();if(error)return NextResponse.json({error:error.message},{status:400});await c.supabase.from('audit_log').insert({actor_id:c.user.id,action:'ad_creative.create',entity_type:'ad_creative',entity_id:data.id,metadata:{campaign_id:data.campaign_id}});return NextResponse.json({creative:data},{status:201});}
